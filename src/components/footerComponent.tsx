import * as elements from 'typed-html';

export function FooterComponent() {
    return (
        <footer class="border-t w-full bg-white py-3 flex justify-center">
            I'm a footer!
        </footer>
    );
}
